import Foundation

struct Constants {
    struct Time {
        static var infinite: VideoTime { VideoTime.infinite }
    }
    struct String {
        /// Fallback chapter label prefix used when a chapter has no title.
        static let chapter = "Chapter"
    }
}
